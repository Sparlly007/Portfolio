import React from 'react';

const Footer = () => {
    return (
        <section>
            <div class="container">
                <div class="footer gray-text">
                    This portfolio was coded in <span>Visual Studio Code</span>.  It was built with <span>React.js </span>  
                    and <span>Bootstrap</span>, and deployed with <span>Github Pages</span>.
                </div>
            </div>
        </section>
    );
};

export default Footer;